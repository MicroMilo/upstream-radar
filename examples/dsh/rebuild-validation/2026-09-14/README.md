# 重建过程中的执行器验证

此目录保存新执行器的真实验证证据。这里的局部检查不代表真实插件批次已经完成重新扫描，也不能替代仓库自动审阅和第二轮更新验收。

隔离环境沿用专用 Colima 虚拟机 `upstream-radar-review-20260913`：无宿主目录挂载、不转发 SSH Agent、不更改默认 Docker context。目标代码只在无宿主挂载的受限容器内运行。

当前检查目标：计划选择的 pnpm 和 profile overrides 是否真的被隔离执行器采用，且实际配置能够被收集并绑定进报告。

## 2026-09-14 06:22 UTC 进展

验证分支 `codex/dsh-rebuild-validation` 已更新到 `c7a65474b3e91d3ecee1c9e3877edd016bf5089b`。本地全量测试为 503 项通过、0 失败；这不是整批插件兼容的证明。

本轮加入并以回归用例约束的行为：

- SDK／ACP 不再借用通用 headless 结果：运行已审阅的作者 CLI 初始化流程，分别采集 application 和 managed profile 的根集合、依赖图与摘要。
- SDK／ACP 结果接收检查实际适配器服务、准确的插件链接、DSH 基础包、运行环境和安装包字节；缺证据不能变成通过。缺少可选平台依赖与缺少必要依赖分开处理。
- 作者声明的 DSH 基线进入独立安装单元，并继续进入同一 Web／TUI 使用方式。当前 8 个插件生成 14 个原生环境单元，不再只保留目标版本的 10 个单元。
- 中断恢复优先接回已运行的任务；构建审阅依据独立保存，不因执行器更新和进程重启丢失。
- 构建许可绑定平台、架构、pnpm 和 profile overrides，重复应用不会堆积同一条许可；缺少环境绑定的旧许可不能直接沿用。
- pnpm 11 的作者 profile 自动安装使用 `PNPM_CONFIG_FROZEN_LOCKFILE=false`。真实隔离最小用例证明：仅设置旧 `npm_config_frozen_lockfile` 仍会出现 `ERR_PNPM_OUTDATED_LOCKFILE`，新设置允许创建 profile 锁文件。

### 已完成的真实运行与边界

[中断恢复及第一轮批次记录](system-batch/recovery-and-v1-batch.json) 保存了 10 个原生单元、10 个 Web／TUI 单元、4 个 SDK／ACP 单元的结果。中断前后的容器 ID 完全相同、尝试次数仍为 1；任务结果收齐，无失败的执行任务或遗失的运行句柄。

必须注意：该轮使用修复前的 adapter v1，SDK／ACP 四项均在冻结锁文件处停止，不能作为初始化兼容或不兼容证据。飞书 Web 的宿主启动则确实出现 `dsh-llm` 缺少 `assertNever` 导出的错误；还需要作者基线对照，不能直接归因给某一上游项目。

此后修复对应 adapter v2。旧报告、容器证据和附件保留在 `system-batch/reports`，不能冒充新版收集器的验收结果。

### 自动闭环验收

[完整系统运行 34813132713](https://github.com/MicroMilo/upstream-radar/actions/runs/34813132713) 已通过 `execute_batch=true` 启动。流程为当前仓库审阅 → 隔离批次 → 根据本批真实构建门槛审阅 → 重试 → 未变化零执行检查。模型凭据只给审阅步骤，目标运行容器不挂载宿主目录、不继承这些凭据。

SDK／ACP 仍为未知、依赖构建门槛未解决、任务丢失或未变化仍执行，都会使最终验收失败。这里记录的是待验收运行，不是完成宣告；最终状态须读取该运行及其 `dsh-rebuild-system-batch` 和 `dsh-rebuild-review` 两份产物。

浏览器精确包版本、认证集成、完整用户任务以及原有 100 插件目录的新一轮覆盖，仍不能由上述局部结果推定完成。

## 2026-09-14 06:42 UTC 进展

此时验证分支为 `a3a6473c698c47babb67fda90ec72000d7efb0bc`，全量回归 506 项通过、0 失败。只更新验证分支，未向主分支推送，保留了工作区原有暂存内容；没有发布 issue／PR。

实际自动运行暴露并修复了三项系统问题：

- 构建审阅曾同时收到模型的 `stop-headless` 和非空构建批准列表，严格校验正确拒绝，但流程缺少纠错。现在最多允许三次通过同一校验器的重新作答；仍不合法则保留待重试任务和拒绝记录，不能自动放宽或归一化为批准。
- 超过读取上限、读取失败和总预算遗漏的仓库文档，原来只在日志出现。现在缺口进入审阅输入指纹、持久化计划和报告；总预算按 UTF-8 字节计算，不再悄悄截断文档。
- 每轮任务额度及镜像枚举顺序，原来会改变执行器身份并使已有证据失效。现在只让真实运行参数和精确镜像／源码身份影响这部分复用。

### 当前代码的真实恢复验收

[恢复证据](recovery-validation/recovery-proof.json) 记录了当前代码下的受控中断：确认 Cloudflare 原生安装容器正在运行后，对我们自己的调度进程发送 SIGTERM，然后重新运行相同入口。前后容器 ID 均为 `0bf5bf8ce5b55ca451c0513bfb6099a26d4fd9fd7f709c104715f53244ddf129`，任务尝试次数保持 1；单任务额度先收齐原运行任务，没有抢先执行其他插件。此前 10 条构建审阅依据跨执行器更新与这次进程重启完整保留。

[额度变更复用证据](recovery-validation/budget-reuse-proof.json) 随后将每轮额度从 1 改为 2，执行器身份不变，已有 Cloudflare 原生证据未重跑，新执行推进了它的 Web 界面和下一个 Context 原生环境。该局部恢复目录目前为 3 个原生单元和 1 个界面单元，不能冒充完整批次验收。

完整运行 `34813132713` 仍使用上一提交 `c7a6547`，06:38 UTC 已进入本批构建审阅后的重试阶段；它的历史审阅步骤已有失败，最终结果和产物还需读取。新提交的审阅运行 `34814396021` 正在排队，不能提前宣称修复已通过真实模型复测或完整闭环已完成。

## 2026-09-14 06:47 UTC 批次结果核验

上述两个运行现已结束：`34813132713` 因最前面的历史构建审阅失败而整体失败，但后续真实批次、本批构建审阅、重试和未变化验收步骤均成功。`34814396021` 使用修复后的 `a3a6473`，真实仓库审阅与三项构建审阅均成功，构建审阅为 3 次有效输出、0 失败。

从已下载的真实产物核实，本批为 8 个插件：

| 阶段 | 本轮执行数 | 当前原生单元 | 当前 Web／TUI 单元 | 当前 SDK／ACP 单元 |
| --- | ---: | ---: | ---: | ---: |
| 第一轮 | 29 | 14 | 11 | 4 |
| 本批构建审阅后的重试 | 6 | 14 | 14 | 4 |
| 未变化重复运行 | 0 | 14 | 14 | 4 |

三条实际安装构建门槛分别为 Better Sidebar 的 `node-pty`、飞书目标 DSH 的 `protobufjs`、飞书作者基线 DSH 的 `protobufjs`，均通过本批独立审阅后重试。最终没有失败任务、遗失运行任务或待运行矩阵项；Cloudflare 声明的 Node 20 仍明确列为执行范围外，不能算已测。

关键对照证据：

- 飞书 `0.19.16` 的 SDK：作者基线 DSH `0.1.0-rc.8` 初始化成功；目标 DSH `0.1.5-rc.2` 初始化失败。独立 managed-profile 图中 `dsh-attachment-local@0.1.5-rc.2` 解析到了 `dsh-attachment@0.1.0-rc.8`，真实子进程报告缺少 `ImageVariantId` 导出。两边都没有向本地模型固定响应服务发送请求；这不是模型生成或飞书认证验收。
- 飞书 ACP：两条 DSH 基线均完成初始化。不能用 ACP 成功替代 SDK 的失败，也不能把 SDK 失败扩大为插件全部不可用。
- Univer：两个作者基线均通过直接 Node peer 检查和 Web 启动；目标 DSH 的 peer 声明范围不满足，但 Web 启动成功。声明不满足与运行崩溃保持分开。
- 飞书 Web：目标 DSH 实际宿主日志含 `dsh-skill` 导入 `dsh-llm` 的 `assertNever` 失败，观察器却记录为 HTTP 404 后的浏览器观察失败，归因仍有待改进；作者基线启动了无凭据扫码向导，浏览器清单没有该插件入口。不能在排除启动配置和旧版清单契约之前，把两者直接作为社区问题。
- Better Sidebar 和 OpenPencil 的浏览器精确 peer 版本仍未得到完整证据，原生 `unknown` 没有因界面启动成功而改成通过。

原始依据位于 [批次摘要](ci-batch-34813132713/batch-output/summary.json)、[本批构建审阅](ci-review-34813132713/build-plans.md)、[独立适配器结果](ci-batch-34813132713/batch-output/adapter-ledger.json) 和 [修复后的真实审阅](ci-review-34814396021/build-review-summary.json)。这些结果证明了一次收集、解决构建门槛和同运行内复用链路；尚不证明修复后全流程、真实输入变化、浏览器版本来源和所有启动条件均已验收。

## 后续全绿批次与当前验收边界

[完整运行 34814985185](https://github.com/MicroMilo/upstream-radar/actions/runs/34814985185) 使用 `a3a6473`，所有步骤成功。已读取其产物，第一轮执行 33 项，本批审阅后再执行 2 项，未变化时执行 0 项；最终仍是 14 个原生环境、14 个 Web／TUI、4 个 SDK／ACP 单元，且各单元结果与前一批一致，无失败任务、遗失任务或待运行矩阵项。不能把 workflow 全绿理解为所有插件兼容：Node 20、浏览器版本及认证集成等覆盖边界仍保留。

随后修复了真实批次暴露的 HTTP 404 归因问题：已有非成功 HTTP 状态但尚未取得宿主退出证据时，保留 `unknown`，阶段归于宿主启动／配置，不能再称为浏览器驱动故障。收集器契约更新为 `dsh-surface/v1alpha11`，旧版报告可读但不能冒充新版执行。[真实日志回放](http-attribution-replay.json) 已验证分类改变；它不是一次新的浏览器执行。

`e9926890352eb94bfd60cea0b479ce4a9b0039b8` 的[完整复测 34818509866](https://github.com/MicroMilo/upstream-radar/actions/runs/34818509866) 已成功结束，产物已下载并读取：首次执行 33 项，构建审阅后执行 2 项，未变化执行 0 项；最终 14 个原生环境、14 个 Web／TUI、4 个 SDK／ACP 单元，没有失败任务或待执行矩阵项。真实飞书 Web 目标 DSH 结果现在正确保留为宿主启动／配置未完成的 `unknown`，不再归因于浏览器驱动。作者基线的客户端入口缺失仍需核对启动条件。

## 浏览器版本来源：新增实现与真实执行

验证分支 `8fd95e99cff0f800892a043aae5772c6d2712518` 新增独立浏览器包来源采集。`dsh-surface/v1alpha12` 配合 `dsh-web-client-contract/2`，要求新版 Web 结果保留实际版本采集尝试；旧报告可读，但不能满足新执行指纹。

- 先在持有 DSH 登录状态的浏览器中取得每个启动模块的真实 HTTP 响应摘要，再与隔离 profile／dlx 目录中独立读取的 `package.json`、客户端文件摘要对应。没有借用 Node 解析版本，也没有把不透明的 boot revision 当 npm 版本。
- 单模块组合响应按[上游固定提交中的字节生成规则](https://github.com/deepseek-ai/deepseek-harness/blob/c291e7961a515f6d7af9304e7fd1d257929aef26/packages/client/modules/src/index.ts)核对完整摘要。不同版本产生同样响应时保留歧义；读取覆盖有缺口时不确认版本。
- 文件读取固定每级目录描述符，不跟随目标软链接。包别名仅在对应实体目录已经独立遍历后才算覆盖；外部别名、客户端路径软链接、文件变化及超限均保留缺口。浏览器请求限制同源，拒绝重定向，并限制每个响应、总字节数和时间。
- [本地真实批次](browser-provenance-batch/state.json) 已取得 Cloudflare Web 的 53 个模块版本；Context 的 Node 22 和 Node 24 Web 均取得 54 个模块版本，包含真实客户端 `dsh-context@0.52.0`，这三次读取的 `gaps` 均为空。Cloudflare 没有声明客户端，所以没有把其宿主工具误认为浏览器模块。
- 以上只证明启动清单中的包来源。React 等静态或打包别名没有因此得到版本证明，`peerVersions` 仍明确为 `partial`；不是所有浏览器 peer 声明均满足，更不是插件全部功能通过。

本地全量测试为 517 项通过。验证分支的[Linux CI 34820751316](https://github.com/MicroMilo/upstream-radar/actions/runs/34820751316) 为 507 项通过，包含全部新增浏览器测试；相差的 10 项是依赖本地 9 月 13 日历史批次文件的两个回归测试文件，未随此次验证分支上传。已逐文件核对，本地与该验证提交的 `src`、构建输入和已有测试内容一致；不能把这两个测试数混用。

新版[完整批次 34820784157](https://github.com/MicroMilo/upstream-radar/actions/runs/34820784157) 已确认运行到真实隔离批次步骤，尚未验收最终结果。后续继续完成：本次完整批次／重试／未变化复用验证，真实插件或仓库更新后的重新审阅与定向执行，以及飞书 Web 的作者启动条件与旧版客户端清单差异。目标保持未完成。

## 34820784157 产物核验与附加启动配置

上述运行现已成功结束，已下载并读取 [最终批次摘要](ci-batch-34820784157/batch-output/summary.json) 及 [真实结果状态](ci-batch-34820784157/batch-output/state.json)。首次执行 33 项，本批构建审阅后补跑 2 项，未变化执行 0 项；最终为 14 个原生环境、14 个 Web／TUI、4 个 SDK／ACP 单元。无失败任务、遗失运行任务或待执行矩阵项；Cloudflare 的 Node 20 仍明确列为执行器范围外，不能声称全部作者环境均已测试。

11 个取得启动清单的 Web 单元均有独立包版本读取，分别确认 42–54 个启动模块版本，读取缺口为空。Better Sidebar、Context、Agy Link、Univer、OpenPencil 的实际客户端版本均已对应；飞书旧 DSH 清单仍缺少插件入口，不能用 42 个宿主模块的版本证据代替它。飞书新 DSH 在宿主启动阶段返回 404，没有版本采集结果。静态／打包别名仍为未完成覆盖，`peerVersions` 保留 `partial`。

真实 SDK／ACP 对照与前一批一致：飞书作者基线 SDK 初始化成功，新 DSH SDK 初始化失败，两侧 ACP 初始化成功。这些结果没有覆盖真实认证、消息或模型任务。

本地新增 `dsh-environment/v5` 和 `dsh-surface/v1alpha13`：仓库明确声明的 disabled／offline 启动配置可作为附加对照，默认启动仍独立保留。配置只接受有原文证据的有限停用开关，不接受凭据、命令或宿主配置。计划、任务、执行参数、指纹、报告、账本和目录汇总保留独立配置，不能用停用桥接的成功填补默认启动缺口。新回归已复现并修复工作流漏传配置，以及目录汇总误覆盖默认启动的问题；真实新审阅和新配置执行尚待下一轮验证，不能用本次 v12 结果代替。

## 2026-09-14 08:49 UTC：新审阅与更新验收进展

验证分支先更新到 `cdc03f379ebd98a1a33cde41342c3c4c857f2298`。其 [真实审阅运行 34823251204](https://github.com/MicroMilo/upstream-radar/actions/runs/34823251204) 已成功，八个仓库全部按 v5 重新审阅，`attempted=8 / planned=8 / failed=0`；立即复查时 `attempted=0`。飞书的附加启动配置确由模型根据 README 和 cordis.patch.yml 的原文选出 `DSH_LARK_DISABLED=1`，没有人工填入审阅结果。配置明确只覆盖“保留插件、停用桥接引擎”，不覆盖消息、认证和嵌套运行时。

随后 `2c35dddc150b7cb4237dbf24f34bd0c132e85878` 接入真实仓库更新回放验收。本地全量回归 523 项通过、0 失败，验证分支 [CI 34824401924](https://github.com/MicroMilo/upstream-radar/actions/runs/34824401924) 成功；逐文件核对的 201 个源码、脚本、已有测试、工作流和构建输入与该提交完全一致。原有真实暂存区未改动，未推送主分支。

[本地采集预检](input-change-preflight/fixture.json) 已通过正常 observer 从 Context 的 `92bfcbd1107cba5fc9887638128be9ba89b63757` 回到真实历史提交 `33fd7ae6801d892ddbee7b76a964a4b2c6ff0416`：只有 Context 的观察内容发生变化，插件仍为 `0.52.0`，采集错误为 0。文档变化没有被伪造成运行时风险事件。该目录只验证准备与采集，携带的旧批次状态不能作为新执行证明。

[完整验收运行 34824401302](https://github.com/MicroMilo/upstream-radar/actions/runs/34824401302) 使用 `2c35ddd`，08:49 UTC 已确认正在真实隔离批次步骤。后续步骤将验证默认／停用桥接两种独立启动配置，以及 Context 历史提交到本轮当前提交的真实变化回放：只重新审阅该仓库、仅执行它的新单元、保持其他七个插件的结果不变，最后零执行复用。必须读取新容器记录和最终 proof 产物才能确认这些步骤完成；目前目标仍未完成。

## 2026-09-14 09:12 UTC：恢复验证与验收脚本修正

[当前代码的中断恢复证明](recovery-current/recovery-proof.json) 已完成：在核对进程归属和容器运行状态后，仅对调度进程发送 SIGTERM，再运行相同批次入口。恢复前后均为容器 `73aabb4edcf159f07bc5a1c0ab9b41fb28d12be918ca00c08bf2327d5fee20fc`，任务尝试次数为 1，容器启动时间一致；单任务额度优先接回原任务，历史构建审阅依据仍在。该次 Better Sidebar 安装／加载成功但依赖覆盖不完整，正确保持 `unknown`，不能算整体兼容。收集器源码摘要为 `af37060e905bef579a342f06e3904c39214374fd980a2abd5cf044e1ee68e36e`。

修复了 CI 只恢复最近一次已完成运行、遇到无产物运行即卡住的问题。回归直接执行工作流的恢复命令，复现最新运行无产物时的失败；新恢复器选择同分支中最近仍有审阅产物的运行，区分产物确实缺失与网络故障，临时解压成功后才交付状态。[真实恢复证明](checkpoint-restore-live-proof.json) 显示从 `34824401924` 取得的观察、环境审阅、构建审阅与此前独立下载的内容逐字节相同。无产物回退和网络错误不回退由回归验证，没有伪称为真实 GitHub 故障。

补查真实原生报告又发现更新回放验收脚本用错包坐标字段：原生报告使用 `artifact.name/version/spec`，Web 报告才使用 `plugin`。已按实际公开报告类型补回归并修正，且拒绝用伪造的顶层 `plugin` 掩盖错误安装包。全量测试重新通过 524 项。上述改动只涉及工作流／验收脚本，不修改当前运行中的收集器。

`34824401302` 在 09:12 UTC 的权威状态为：主批次、构建审阅、重试、初次未变化检查、两次真实 Context 仓库采集和审阅均成功；历史 Context 执行后的验收步骤失败，前进到当前提交后的执行仍在进行。失败日志和原始产物尚待收齐核对，不能提前把失败原因或更新验收视为已确认完成。

## 2026-09-14 09:35 UTC：真实回放核验与持续入口差异

`34824401302` 已终止为失败。已读取两处失败日志，均明确为更新验收脚本读取不存在的顶层 `report.plugin`；并非 Context 执行失败。当前修正脚本对已下载的两份实际产物重新核验成功，分别留下 [历史提交执行证明](ci-batch-34824401302/input-change-before/execution-proof.json) 和 [向前更新执行证明](ci-batch-34824401302/input-change-after/execution-proof.json)。两次均只执行 Context 的 4 个独立容器，其他 7 个插件的结果不变。原 CI 未执行完最后的未变化检查，因此这次事后核验不能冒充完整 CI 成功。

该真实主批次首次执行 39 项、构建审阅后再执行 2 项，未变化复跑为 0。最终为 16 个原生单元、18 个 Web／TUI、4 个 SDK／ACP 单元，无失败任务、遗失运行任务和待运行单元；Node 20 仍为明确未覆盖。四个飞书 Web 对照保持默认与停用桥接独立：旧 DSH 两种启动均缺少客户端入口；新 DSH 默认启动保留宿主阶段 `unknown`，停用桥接时明确记录宿主退出 1。停用配置没有被用来覆盖默认结果。

修正验收脚本后的完整运行 [34827492907](https://github.com/MicroMilo/upstream-radar/actions/runs/34827492907) 已实际启动，使用 `b9c22f601dd32740449dbfe4b254155a2c02cee5`。此处不提前宣布它成功。

对持续入口的补查发现了批次验收未覆盖的系统差异：正常 `write-dsh-surface-plan.mjs` 只生成 11 个目标 DSH 单元，遗漏作者基线的 7 个单元。另一个入口级回归证明：当新安装包已审阅但尚未取得安装证据时，它仍可能调度旧包的界面测试。

这两项现已修复：正常入口和批次入口共用作者基线展开、精确当前来源筛选。回归先观察到仅 2 个目标版本单元、再观察到错误沿用旧包的 4 个单元，修正后分别为 4 个完整对照和 0 个执行／4 个等待新来源。完整回归为 525 项通过。[使用真实批次证据生成的正常入口计划](normal-surface-plan-current.json) 现为 18 个单元、0 个阻塞；逐项与真实批次核对，包坐标、DSH、来源、配置及两种指纹全部相同。这是规划与复用一致性的证明，不是另外执行了 18 个容器。

仍未完成：原有定时流水线没有接入独立 SDK／ACP 执行和对应目录汇总；它们目前仅在批次入口闭环。需要补齐此项，不能用批次成功替代整个系统完成。

## 2026-09-14 10:31 UTC：发布并发验收修复与正常 SDK／ACP 链路

完整运行 `34827492907` 最终为失败，唯一失败步骤是历史源码回放的观察验收：在该步骤运行前，Context 于 `2026-09-14T09:45:29.674Z` 实际发布 `0.52.1`，旧脚本却要求 npm 仍停在初始的 `0.52.0`。实际 observer 捕获了版本及 integrity 更新，后续只审阅 Context、执行其 4 个容器，其他 7 个插件未重跑；最终未变化检查为 0 执行。

更严重的是旧验收脚本会在观察验收失败后继续生成执行证明，并把初始 fixture 的旧包名写入证明。现已增加 `observed-change.json`：核对源提交、前后实际包身份、对应 meaningful 事件及观察摘要；后续审阅与执行必须绑定已验收观察，任务和原始报告必须对应实际包。工作流的回放步骤也改为前序成功才继续。无对应事件的版本变化、旧任务／旧报告成对冒充新包、观察记录被更换，都会拒绝。

将原始产物复制到独立目录后，新检查对真实发布场景的观察、审阅、执行三阶段全部通过。[新执行证明](reverified-release-34827492907/execution-proof.json) 正确记录 `0.52.0 → 0.52.1`、执行 4 项、其他 7 个插件不变；旧错误证明保留为同目录的 `original-execution-proof.json`。原始 CI 下载目录没有被改写；这次离线核验不是新的容器运行，也不把失败 CI 改称成功。

[正常独立任务验证 34831181116](https://github.com/MicroMilo/upstream-radar/actions/runs/34831181116) 已成功执行四个真实容器，使用与正常 observer 相同的 SDK／ACP 工作流。所有容器为 `10001:10001`、只读根、无挂载并已退出；没有模型或 GitHub 密钥进入目标执行。SDK 在目标 DSH `0.1.5-rc.2` 下初始化失败，在作者基线 `0.1.0-rc.8` 下成功；ACP 两侧均完成初始化。四项均保留独立 application／profile 依赖图，不覆盖飞书认证、模型生成和用户业务任务。

真实报告通过正常 `merge-dsh-adapter-ledger.mjs` 入账，[合并摘要](normal-adapter-merge-summary-34831181116.json) 为 4 accepted、0 missing、0 rejected；相同事实重新运行正常 adapter 规划命令得到 0 执行、0 blocked。原始报告在 `ci-adapter-34831181116`，新账本为 [normal-adapter-ledger-34831181116.json](normal-adapter-ledger-34831181116.json)。

目录汇总进一步修复三处缺口：加入 SDK／ACP 的结果、限定范围和独立图摘要；保留被任务编号过滤误删的作者基线安装／界面证据；要求每个当前推荐环境对应仍有效的源码和执行约定，旧的绿色安装结果不能覆盖新要求。所有正常汇总入口都传递 adapter 账本及精确构建审阅，adapter 合并在 Web／TUI 合并结束后写入，避免两个任务同时覆盖同一目录文件。SDK 初始化通过不会升级为认证集成通过。

[正常入口生成的目录视图](normal-feed-34831181116.md) 来自已下载的真实安装／界面证据和上述新 adapter 报告，包含 15 个安装、17 个界面、4 个 adapter 单元，四个 adapter 的结果、图摘要与原始报告逐项一致。这个目录视图有 7 个插件：固定目录树中的 Feishu 条目已另行核验，Cloudflare Browser Run 在该目录中没有条目，因此明确留在八插件批次而不伪造目录链接。没有重扫原来的 100 插件；旧目录仅更新 schema／说明，原观察时间和版本未刷新。

当前验证提交为 `ae70fd5df3513ecf5d1d9b0a821a598fd3506bd6`，本地完整回归 531 项通过、0 失败。仅推送 `codex/dsh-rebuild-validation`，真实暂存区仍未改动，未推送默认分支或发布 issue／PR。[新的完整八插件验收 34833364736](https://github.com/MicroMilo/upstream-radar/actions/runs/34833364736) 已启动；它新增运行同一目录汇总命令并核对账本完整性。当前尚不能把本次完整运行或当前代码的恢复复验判为完成。

## 2026-09-14 10:47 UTC：当前恢复结果与汇总覆盖复查

`ae70fd5` 的[真实恢复复验](recovery-ae70fd5/recovery-proof.json) 已完成。第一项原生安装在故障注入前正常结束，因此没有伪称中断了它。随后在 Better Sidebar Web 容器实际处于 running 状态时，对本次直接创建的调度子进程 `79714` 发送 SIGTERM，并确认子进程以该信号退出。恢复仍使用容器 `c1aa7f339f4eb0e9a778207e442652090b14499453bf22ad25fce2aa56983efa`，尝试次数为 1、启动时间未变，单任务额度优先收齐该任务，历史构建审阅依据保留。恢复后的 Web 入口观察为 compatible；原生依赖覆盖仍为 unknown，不能升级为整个插件兼容。源码摘要 `10e84fff6796c39d45e90afca7c81d5c739fc7f27ea128669ecefe5fa05958d0` 只对应这次恢复的代码。

后续覆盖复查又复现了一个汇总缺口：原生来源仍有效时，旧 Web 执行约定也可能被计入当前覆盖。现在目录汇总通过同一个 surface planner 比较当前执行约定、profile 环境、原生／Web 专用构建审阅。旧约定保留为历史证据，但不能填补当前 Web 要求；无效构建审阅输入即使没有可用界面单元也会报错。四个正常汇总入口均传入 Web／TUI 专用构建审阅。

同时修复了汇总默认只构造 x64 对照约定的问题：Linux arm64 的真实证据按 arm64 约定核对，不再被误报为缺少当前覆盖。[使用上述实际恢复结果生成的视图](recovery-ae70fd5/catalog-feed.json) 中 Better Sidebar 的安装和 Web 证据均保留 arm64；其所选 smoke 单元完整，但整体状态仍为 needs-review。该视图的其他目标没有在这次局部恢复中执行，仍明确未覆盖。

上述最终汇总修正已推送为 `86ac6c4ca25cd11e105dc2c1a5781ccff9a76ecd`，本地全量回归 532 项通过、0 失败；没有改变目标收集器的初始化／浏览器观察语义。`34833364736` 属于前一提交，10:46 UTC 仍在本批构建审阅后的重试阶段；不能替代最新提交的完整验收。[最新提交的完整验收 34834786668](https://github.com/MicroMilo/upstream-radar/actions/runs/34834786668) 已发起，等待同一并发组的前次运行结束。目标保持未完成，后续须读取两次运行的原始产物及最新提交的全部验收结果。

## 2026-09-14 11:14 UTC：固定提交证据复用、失败状态与恢复复验

`34833364736` 最终失败于“未变化仓库证据复用”检查。[首次审阅摘要](ci-review-34833364736/review-summary.json) 为 0 次调用，[立即复查](ci-review-34833364736/review-repeat-summary.json) 却变为 1 次审阅；日志出现文档抓取超时，最后回答只属于 Context，并加入了收集超时的覆盖缺口。同一观察快照和固定提交被重复抓取，网络波动使已完成审阅的输入退化，这是我们的收集与复用缺陷。

当前修复提交为 `d75b7648f80d5d09e5583ce1e8b861609b6b4eb8`，只推送验证分支。规划器在模型交付前原子保存固定提交的文档缓存，绑定仓库、提交、包路径、基线角色、收集器代码和字节摘要；只保留当前批次，最多 101 份集合、64 MiB。文件大小／数量省略仍是明确缺口；网络和解析失败不会被冻结为完整集合。提交、发布包和收集器变化仍有对应的重新收集或审阅行为。正常持续入口保存该缓存，历史版本回放保留未变化的缓存。还补上了普通 observer 最终状态遗漏环境／构建审阅失败的问题：保留成功部分，但整轮不能因此显示成功。

本地完整回归为 **540 项通过、0 失败**。新增测试直接运行正常 CLI，覆盖跨进程断网复用、大小限制缺口、插件／DSH 提交变化、发布包变化、收集器变化、损坏／软链接／越界缓存，以及部分文件抓取和 Git 树解析失败后的恢复；另直接执行最终状态检查的 shell，验证各类审阅失败和正常／跳过路径。

[八个真实插件的缓存证明](cache-live-batch/cache-proof.json) 使用 `34834786668` 的真实完成审阅，重新从 GitHub 收集相同固定提交，再在第二次运行人为禁止全部网络请求。两次都是 0 次模型调用；八个候选输入和审阅结果逐字节不变；9 份固定提交集合被保留。Better Sidebar、TUI、OpenPencil 的 2／4／11 条收集缺口没有消失。这是明确的故障注入实验，不是声称 GitHub 实际断网，也不是新跑了插件容器。

[当前代码的恢复证明](recovery-immutable-evidence/recovery-proof.json) 记录了直接拥有的调度子进程 `87374` 在容器 running 时收到 SIGTERM 并退出。恢复后仍接回容器 `9535b01626df12de41ee7864f9167d93b0817e9771a0c0f8dd544b657d2d6b43`，尝试次数 1、启动时间不变；无失败或遗失任务，原历史构建审阅项仍保留。安装／加载成功而依赖覆盖不足，结果为 `unknown`。镜像的 79 个源码／构建输入逐个与 `d75b764` 对应，摘要为 `9e101c33d74b12db40db8f03955dd4db5740f68abfcbb7cb3475f303bc6d3ca4`。这只验证真实 Linux arm64 中断恢复，不冒充整个 arm64 批次完成。

`34834786668` 已完成但失败在历史 Context 仓库审阅步骤（退出 2）；主批次、构建重试、未变化执行检查和目录汇总步骤均成功。审阅产物已下载，批次及历史审阅原始产物仍在下载，具体审阅失败原因尚待核对。[修复后的完整运行 34836584188](https://github.com/MicroMilo/upstream-radar/actions/runs/34836584188) 已确认使用 `d75b764`，并实际进入隔离批次步骤。目标保持未完成：仍须检查历史审阅失败的根因，以及最新运行的实际汇总、选择性更新和零重复执行证明。

## 2026-09-14 11:25 UTC：真实版本表审阅缺口与 v6

`34834786668` 的原始批次产物已经下载。首次执行 39 项，本批构建审阅后重试 2 项，未变化复跑为 0；最终有 16 个安装、18 个 Web／TUI、4 个 SDK／ACP 单元，无失败／遗失任务，下一轮矩阵为空。Cloudflare 的 Node 20 仍明确不可执行；浏览器静态 peer 版本和真实认证任务仍未覆盖。正常目录汇总已在这个真实 CI 中成功，7 个目录内插件保留 15 个安装、17 个界面和 4 个 adapter 单元；第 8 个 Cloudflare 仍在完整批次中，没有伪造目录条目。

失败集中在历史 Context 的唯一审阅任务：[原始回答](ci-batch-34834786668/input-change-before/review/recommendations.json.attempts.json) 的前两次错误拼接了 `cordis.patch.yml` 注释，正确被拒绝。第三次引用已经逐字匹配，但引用的 DSH 版本表行没有重复出现表头的 `dsh release` 字样。旧验证器只在引文内部搜索 DSH，因此错误拒绝了作者声明的 `0.1.2-rc.1`、`0.1.3-alpha.2`、`0.1.5-rc.1`。

现在按同一文档内的 Markdown 表头、分隔行和列数确认版本归属。只有确切 DSH 版本列里的精确版本可以作为该插件的基线；其他包的列、断开的表格、不匹配的引用、范围或 DSH 自己的文档不能冒充。相同引文出现在多个表格时，查找真正支持它的 DSH 表格，而非只看第一个出现位置。为防止沿用遗漏基线或错误列语义的旧审阅，约定升为 `dsh-environment/v6`，旧结果仅保留为历史。

[原始回答复验](table-review-reproduction/revalidation-proof.json) 通过正常收集器重新读取固定历史提交 `33fd7ae6801d892ddbee7b76a964a4b2c6ff0416`，没有修改回答：前两次仍被拒绝，第三次通过并保留上述三个基线。该目录没有配置模型，只有待审阅任务和离线校验，不能当作新完成的八插件审阅。

最新提交为 `0f8f25967881e37fd4efab031ae0900751bdba23`，本地全量回归 **545 项通过、0 失败**，只推送验证分支。上一提交的 `34836584188` 仍在实际运行，不能据此证明新 v6 语义验收完成。下一步先完成验证分支自动触发的八插件 v6 审阅，再使用该真实审阅重新执行完整批次和选择性更新验收。此前恢复证明绑定 `d75b764`，当前 v6 会改变镜像源码摘要，最终还需要对应当前代码的恢复证据；目标保持未完成。

v6 自动审阅运行为 [34838074785](https://github.com/MicroMilo/upstream-radar/actions/runs/34838074785)，已确认提交为 `0f8f259`，等待同一并发组中的 `34836584188` 结束。尚未另行发起 v6 完整批次；应先核对这次真实审阅的输出，再发起 `execute_batch=true`，避免在审阅尚未完成时重复排队。

## 2026-09-14 11:37 UTC：统一多行表格引用校验

补查同一规则发现，多行引文会绕过单行表格分支，退回旧的关键词匹配，因此包含 DSH 表头的整张表仍可能把“其他包”列的版本错误用作 DSH 基线。新回归先复现该误接受，再改为对引文中每个完整表格行执行同一列归属检查：只接受引文实际包含、且处在 DSH 版本列中的精确版本；不能借用文档其他位置未引用的正确表行。

审阅约定随之升为 `dsh-environment/v7`，v5 和 v6 结果均不能继续作为当前执行依据。最新提交 `2f6d3da15b71b2f214ec92c91bf23a8d664df5ce` 已推送到验证分支，本地 **546 项回归通过、0 失败**。未启动的 v6 任务被同一 GitHub 并发组中的新提交替换；[v7 自动审阅 34838893139](https://github.com/MicroMilo/upstream-radar/actions/runs/34838893139) 已排队，未重复提交完整批次。旧提交 `34836584188` 已进入最后的 Context 定向执行及未变化复查阶段；仍需下载原始证明，并在 v7 审阅成功后完成当前版本完整验收。

## 2026-09-14 11:59 UTC：防止已知作者基线静默丢失

`34836584188` 已成功完成，原始批次产物保存在 `ci-batch-34836584188`。首次执行 39 项、构建审阅后重试 2 项、未变化复跑 0 项，最终账本为 16 个原生、18 个 Web／TUI 和 4 个 SDK／ACP 单元，无失败和遗失任务。两次真实 Context 提交回放均执行其 4 个容器，另外 7 个插件和执行器不变；这是旧 `d75b764` 的完整结果，不是新审阅语义的验收。

`34838893139` 的 v7 审阅成功完成 8 项，立即重复审阅为 0；Context 的三个版本表基线已保留。但逐项核对发现 TUI 的 `0.1.5-rc.1` 作者基线被遗漏，导致安装计划由完整的 22 项变为 20 项。相同 TUI 提交 `8351d9d1dcbd2b2251705d31cf82735bf5b8922a` 的 `pnpm-workspace.yaml` 仍明确称其为 primary validated line；不能以 CI 绿色接受这一缩减，因此没有执行 v7 的完整批次。

当前 `dsh-environment/v8` 将同一插件包坐标、仓库和固定提交中的旧基线，按当前字节和当前校验规则重新验证。新推荐遗漏仍有根据的基线时进入三次有界纠正，绝不直接复制旧模型结论；持续失败则保留待处理任务，不发布缩小计划。被删除的引文、不同来源、当前规则已拒绝的旧结论不会被锁定；冲突可以明确保留为 `insufficient-evidence`。这保护已有事实，不假称完全解决首次审阅的所有漏项。

同时修复作者要求的来源边界：`dsh-source-manifest`、`dsh-published-manifest` 和 `dsh-repository/` 文档不能证明插件作者的包管理器、overrides、工作流、启动配置或 DSH 基线。TDD 回归先复现宿主要求被错误接收，以及正常 CLI 静默清空作者基线的问题；修正后验证纠正、计划保留两个 DSH 版本、重复零调用、三次失败保持待处理和下一轮恢复。完整回归为 **550 项通过、0 失败**，最后的诊断文本调整后针对性回归为 43 项通过。

[真实回答复验](retained-baseline-reproduction/revalidation-proof.json) 使用未修改的 v7 模型输出与仍有效的 v5 引文，新规则正确拒绝遗漏；这里只做离线校验，没有伪造新的模型审阅或执行证据。提交 `58b3a0e7de6fd60fefc963ec9202bb6cb98e2d61` 已仅推送验证分支，[v8 实际审阅 34840887155](https://github.com/MicroMilo/upstream-radar/actions/runs/34840887155) 正在运行。仍需检查八项新回答，再完成当前代码完整批次及恢复复验；目标未完成。

## 2026-09-14 12:58 UTC：首次审阅完整性、TUI 名称与有界纠错

v8 审阅最终成功，但 TUI 基线仍被遗漏：最近的历史审阅也已丢失它，单靠历史保留规则无法保护首次审阅。Cloudflare 还把 `@deepseek-ai/dsh-tools: ^0.1.5-rc.2` 的依赖范围误当成已验证 DSH 版本。v9 增加基于插件文档的保守完整性检查，要求明确的已验证 DSH 版本声明进入比较计划；版本范围、否定和计划性措辞不能当成已验证事实。这不是完整自然语言分析的替代，其他来源与未收集文件仍有明确边界。[无历史输入的真实回答复验](first-review-reproduction/revalidation-proof.json) 仅拒绝这两个错误，其他六个原始回答通过。

[v9 审阅 34842318956](https://github.com/MicroMilo/upstream-radar/actions/runs/34842318956) 完成八项，重复零调用，安装计划 22 项。但 TUI 没有保留作者明确使用的 `dsh-tui` profile 名称，规划器生成了另一个名字；没有因此启动 v9 完整批次。v10 要求已选择 TUI 的回答保留该插件字面安装命令中的名称，不执行命令、不接受其他包或宿主文档作为依据。[原始 v9 回答复验](named-profile-reproduction/revalidation-proof.json) 正确拒绝缺失名称的 TUI 回答，其他七个通过。

[v10 审阅 34843841674](https://github.com/MicroMilo/upstream-radar/actions/runs/34843841674) 完成七项，TUI 的 `0.1.5-rc.1` 基线和 `dsh-tui` 名称均保留；飞书在三次有界纠正后仍不合法，保持待处理。此时产物却包含旧的重复审阅成功摘要，实际重复步骤已跳过。这是 checkpoint 恢复范围的缺陷，现已改为只恢复六种持久输入／历史文件，派生摘要与计划必须由本轮生成。[实际恢复校验](checkpoint-scope-v10-proof.json) 证明六个文件字节与原始产物一致，七项当前审阅和飞书待办保留，没有继承旧成功摘要。

修复后的 [34844914573](https://github.com/MicroMilo/upstream-radar/actions/runs/34844914573) 确实只重试飞书，也没有生成被跳过的重复审阅摘要；但飞书再次失败。未修改的原始回答显示同时存在 overrides 引用值不符、启动配置错误归入 TUI、Node 22 被标成作者推荐三个独立错误。旧校验器每次只反馈第一项，浪费有限纠错机会。

当前提交 `ace7ecb4abd451138a5c0495a44b213190fda7d0` 将独立语义错误一起反馈，最多显示四项、总长度不超过 1024 字符；结构不合法仍立即拒绝，三次重试上限和接受规则不变，审阅约定仍为 v10。正常 CLI 回归证明错误回答被拒绝、下一次得到全部反馈后纠正、正确名称进入界面计划、重复零调用；全量 **559 项通过、0 失败**。真实失败回答的第二次输出现在一次显示三项错误，第三次仍显示剩余两项；没有手工改写模型结论。

[当前自动续跑 34846300366](https://github.com/MicroMilo/upstream-radar/actions/runs/34846300366) 已确认使用该提交并实际运行。完整新批次与当前代码恢复实验尚未执行，不提前宣布闭环。v10 的 Cloudflare 推荐同时保留 Node 20 最低声明与 Node 22 CI 依据，Node 20 仍是不可执行的明确覆盖缺口；旧 v9 只选 Node 22 的计划不是本轮依据。原有 100 插件目录没有重新扫描，未发布 issue／PR，未推送主分支。

## 2026-09-14 13:08 UTC：不能通过删除对照解决审阅错误

`34846300366` 最终成功，真实审阅只处理飞书一项，立即重复零调用。其三次回答分别为引用数越界、两项合并错误、合法回答，证明合并反馈确实进入了真实纠错链路。八项均为 v10，安装计划 22 项，只有 Cloudflare Node 20 的执行范围缺口。

但原始合法回答把 `DSH_LARK_DISABLED=1` 启动对照删掉了，理由是停止桥接引擎不能代表完整集成。这个范围说明本身正确，却不构成删除附加对照的理由：相同固定插件提交的 README 明确写着在启动 profile 前设置该开关，插件仍加载但桥接停止。默认启动和停用对照必须各自保留、分别解释，不能用其中一个替代另一个。因此没有启动该版本的完整批次。

v11 增加有界的首次审阅检查：只检查插件文档中明确写在启动前使用的允许禁用／离线开关，保留字面引用；宿主文档、源码样例、否定／弃用指令、非允许开关、超长资料不会被当成这个检查的执行要求。实际平面和范围仍由仓库审阅给出，无法确定可留为 `insufficient-evidence`，不会由扫描器自动执行命令。还修复了合法 Markdown 反引号后的值边界被拒绝的问题，值前缀和其他变量名仍拒绝。

[八个未修改的真实回答复验](startup-reproduction/revalidation-proof.json) 按各自原始 CI 候选输入逐项核对，只拒绝遗漏停用对照的飞书回答，其他七项通过。新增正常 CLI 回归证明首次遗漏被拒绝、纠正后生成默认和停用两个 Web 对照、重复零调用。全量 **563 项通过、0 失败**；审阅约定升为 v11，v10 只能保留为历史，不能直接授权新计划。

提交 `4fdb823e6b71b7aedaf827f3db2e0534ffa2a71d` 已仅推送验证分支。[真实 v11 审阅 34847244372](https://github.com/MicroMilo/upstream-radar/actions/runs/34847244372) 正在运行；完整 v11 批次、当前代码恢复证明仍待执行。准备好的恢复驱动位于 `recovery-v11/run.mjs`，此时尚未启动目标容器。

## 2026-09-14 13:40 UTC：v11 审阅完成与当前代码真实恢复

`34847244372` 成功完成八个真实仓库的 v11 审阅，立即重复为零调用，当前输入指纹逐项匹配，没有待处理审阅。作者 DSH 基线、TUI 名称和飞书默认 SDK 流程均保留。飞书保留默认 Web 启动，并增加文档中的桥接停用及 guardian 停用两个独立配置；这不代表已验证飞书认证或 guardian 服务本身。安装计划为 22 项，Cloudflare Node 20 仍是明确的不可执行范围缺口。

[当前完整八插件验收 34847607310](https://github.com/MicroMilo/upstream-radar/actions/runs/34847607310) 已以 `execute_batch=true` 发起，绑定提交 `4fdb823e6b71b7aedaf827f3db2e0534ffa2a71d`。本地和该 CI 使用同一源提交；本地 79 个镜像输入及批次调度脚本的 Git blob 均逐项一致。13:38 UTC 的 GitHub 状态仍为隔离主批次执行中，尚未取得整批结果、输入变化回放和最终零重复执行证明。

第一次恢复实验保存在 `recovery-v11`：确实中断并接回同一容器，单次尝试的结果已接受；但验收脚本错误要求新观察与旧观察整条 JSON 一致，因 `observedAt` 和临时路径导致的 profile 锁文件字节更新而报错。原始实验和错误脚本保留，没有重写成成功。最新观察理应可以更新这些值；历史原始证据以及仍然适用的精确构建许可才需要保留。

修正验收方式后，在独立 `recovery-v11-checked` 目录重新执行真实实验，[恢复证明](recovery-v11-checked/recovery-proof.json) 已通过：直接创建的调度进程 `11873` 在实际容器 running 时收到 SIGTERM 并退出；恢复接回 `53bfa566d225b99f667f3dd3a14677c98b7d6b8ce0dfdf127c07e0ac310a5262`，尝试次数 1、容器启动时间不变。中断前保存的状态仍包含原始构建依据；恢复前后通过正常 `applyDshHeadlessAgentPlans` 得到的许可完全相同，绑定 `node-pty`、插件 SHA-256、DSH、Node、架构和 profile 环境。原始历史快照独立保存，新结果不会冒充旧字节。

该实验执行并收齐 1 个 Linux arm64 原生单元，失败／遗失任务均为 0；无遗留目标容器，默认 Docker context 仍为 `colima`。报告为 `unknown`：安装及加载成功，仍缺 4 个 Node peer 和 5 条依赖边，不能改写为整个插件兼容。源码摘要为 `0162f53efb827d7018823621166cc75c4d74431c31ed45ab072f083e9ea13aee`；证明只覆盖当前代码的单任务恢复，不替代完整八插件验收。容器与操作者的时间来自各自时钟，仅比较同一容器前后的启动时间，不用跨时钟差值宣称执行耗时。

当前源代码仍为上述提交，完整回归 563 项通过；未推送主分支、未发布 issue／PR、未重扫原有 100 插件目录。目标保持未完成，下一步读取运行 `34847607310` 的终态及原始产物，逐项核对实际批次、作者基线与配置覆盖、定向更新、未变化复用和统一汇总。

## 2026-09-15 02:26 UTC：真实批次暴露空日志报告错误，先修收集器

`34847607310` 已确认终态为失败。原始批次产物完整下载，压缩包大小为 53,951,485 字节，SHA-256 为 `56f324cb43dc041bb092628b74716273889a60f497445cd147a4432fd0163ef3`，与 GitHub 产物元数据一致。中途两次普通下载确实因 EOF 终止；后续有界断点续传保留部分字节并完成摘要验证，没有把下载超时当作 CI 仍在运行或重新发起批次的理由。

第一次批次执行 53 项，构建审阅后重试 3 项，预期零执行的复跑却再次执行了 1 项，因此验收正确失败；目录汇总与两次真实输入变化回放均跳过。最终虽收齐 22 个原生、26 个 Web／TUI 和 4 个 SDK／ACP 单元，也不能把这个失败运行改写成完整闭环。

额外执行均来自 `dsh-context@0.52.1 × DSH 0.1.3-alpha.2 × Node 24 × Web`。前两次报告的 `stages.host.detail` 是空字符串，不是超长文本：宿主日志尚未产生，收集器写出了不满足自身接收规则的字段。HTTP 404 和启动未完成的原始观察已存在，但报告被整体拒收；第三次恰好取得非空宿主日志才被接受。这是 Radar 的报告生成错误，不是应交给插件作者修复的缺陷。

修复先通过正常报告生成、账本合并与后续规划链路复现：空／全空白日志现在使用已有的 HTTP 状态或已观察退出说明，仍为原来的未知或宿主失败结果；入站解析器仍拒绝空字段。另一个边界回归确认浏览器错误在截断后加前缀仍可能超过 2048 字符，现改为约束完整说明。非空宿主日志的限长与临时登录 token 脱敏保持有效。全量 **566 项通过、0 失败**。

[原始报告离线复验](web-error-reproduction/revalidation-proof.json) 保留三份历史报告的 SHA-256，不修改原文件。前两份仅在派生副本的 `stages.host.detail` 补入原有说明后通过同一接收器；第三份有效报告完全不变。三份结果均为 `unknown`。这只是收集器／接收器回归，不是重新运行了插件，也不是新兼容性证据。

本修复不改变合法报告的判定规则，审阅约定继续使用 v11。新的执行器源码摘要会变化，仍须运行修复后的完整八插件批次、定向更新、未变化复用，以及对应当前代码的真实中断恢复；目标保持未完成。
