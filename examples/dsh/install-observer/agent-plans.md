# DSH headless Agent review

Updated: 2026-08-29T13:00:13.995Z

The Agent reads bounded repository evidence and the latest isolated headless result. There is no static environment-planning fallback. Only an exact observed build-package name can reach the no-secret retry runner.

- Current review set: 23
- Agent-reviewed: 23
- Agent failures awaiting retry: 0

| Case | Previous evidence | Agent action | Classification | Retained build policy |
| --- | --- | --- | --- | --- |
| `better-sidebar-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin's peer contract is not satisfied by the headless DSH runtime: @deepseek-ai/dsh-client-ui-primitives and @deepseek-ai/dsh-client-ui-slots are missing, and react-dom is mismatched (resolved 19.2.8 vs required ^18.2.0). The observed result is peer-contract-incompatible, not a build-approval issue. No build packages are required, so a retry cannot resolve the missing peer dependencies. The headless profile cannot add or modify peer dependencies, and the plugin requires a web platform (dsh.client.platform: web) which is outside the headless execution plane. |
| `dsh-agency-agents-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin loaded but the DSH runtime did not resolve the peer dependency @deepseek-ai/dsh-client-ui-slots@^0.1.0-rc.5. The manifest declares it as a peer dependency and the runtime graph shows it missing. No build packages are required, so a retry cannot fix the missing peer. The plugin also declares a web platform client, which is outside the headless execution plane. |
| `dsh-archive-manager-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin's peer dependency @deepseek-ai/dsh-client-ui-primitives is missing in the runtime graph, but the manifest declares it as a peer dependency and the plugin uses runtime-import-observed. The DSH runtime (0.1.1-rc.2) does not provide this package, and the plugin requires a Web platform (dsh.client.platform: web) which is not available in the headless profile. No build packages are required, so a retry cannot resolve the missing peer dependency. |
| `dsh-awiki-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin artifact installed and loaded, but the DSH runtime could not resolve two peer dependencies: @deepseek-ai/dsh-client-ui-primitives and @deepseek-ai/dsh-client-ui-slots. The runtime graph shows these as missing, and the plugin's manifest declares them as peer dependencies. The headless profile cannot provide these UI packages because they are part of the Web/TUI plane, which is not available in this execution environment. No build packages are required, so a retry would not resolve the missing runtime dependencies. |
| `dsh-codex-connect-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin's peer dependency @deepseek-ai/dsh-client-ui-primitives@0.1.1-rc.2 is missing from the runtime graph, and the plugin declares a web client platform with injects that require a Web/TUI plane. The headless profile cannot resolve this peer dependency because it is a UI package not provided by the headless runtime. No build packages are required, so a retry cannot fix the missing peer. The issue is a headless contract limitation. |
| `dsh-commandcode-provider-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin's peer contract requires @deepseek-ai/dsh-client-ui-primitives and @deepseek-ai/dsh-client-ui-slots, which are not resolved by the DSH runtime. The observed result is peer-contract-incompatible with missing runtime-imported peer dependencies. No build packages are required, so a retry cannot resolve the missing peers. The issue is a headless contract mismatch: the plugin expects UI primitives and slots that the headless runtime does not provide. |
| `dsh-config-manager-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin installed and loaded, but the DSH runtime did not resolve @deepseek-ai/dsh-client-ui-slots@^0.1.0-rc.6, which is required as a peer dependency and used via runtime import. The headless profile cannot provide the missing peer package because it is not a build dependency and no build approval can add it. The issue is a runtime contract mismatch, not a build approval problem. |
| `dsh-full-remote-node22` | `peer-contract-incompatible` | `stop-headless` | `different-plane` | none |
|  |  |  |  | The plugin requires a web profile and injects client UI slots, but the headless DSH profile cannot resolve the peer dependency @deepseek-ai/dsh-client-ui-slots. The observed result is peer-contract-incompatible due to a missing peer, not a build approval issue. No build packages are required, and the failure is due to the headless environment lacking the web client runtime, which cannot be fixed by approving builds. |
| `dsh-im-connect-node22` | `build-approval-required` | `retry-headless` | `build-approval` | approve `protobufjs` |
|  |  |  |  | The observed result is build-approval-required for protobufjs. The repository evidence shows the plugin depends on @larksuiteoapi/node-sdk, which is known to depend on protobufjs. The lockfile graph includes protobufjs as a transitive dependency. Approving protobufjs is justified as it is a well-known pure JavaScript library with no native build requirements, and the headless profile can build it. No previous approvals exist, so this retry adds protobufjs only. |
| `dsh-mneme-node22` | `build-approval-required` | `retry-headless` | `build-approval` | approve `onnxruntime-node`, `protobufjs`, `sharp` |
|  |  |  |  | The plugin requires build approval for onnxruntime-node, protobufjs, and sharp. The repository evidence (README) indicates these are used for optional local embedding and reranking features (ONNX runtime, protobuf for model files, sharp for image processing). The manifest shows @huggingface/transformers as a dependency, which pulls in onnxruntime-node. The peer dependencies are satisfied. Approving these builds is justified as they are part of the plugin's declared functionality and the headless profile can proceed. |
| `dsh-mnemon-node22` | `peer-contract-incompatible` | `stop-headless` | `different-plane` | none |
|  |  |  |  | The plugin declares a web client platform and injects web-only UI packages, but the headless profile cannot resolve the required @deepseek-ai/dsh-client-ui-primitives peer dependency. The observed runtime graph shows the peer is missing and react-dom is mismatched (19.2.8 vs required ^18.2.0). No build packages are required, so a retry cannot fix the peer contract issue. The plugin's web UI dependencies require a Web/TUI plane, which is not available in this milestone. |
| `dsh-remote-node22` | `unknown` | `stop-headless` | `different-plane` | approve `cpu-features`, `node-pty`, `ssh2` |
|  |  |  |  | The plugin artifact installed and loaded, but the runtime graph has 2 unresolved peer dependencies from dsh-better-sidebar: @deepseek-ai/dsh-client-ui-primitives and @deepseek-ai/dsh-client-ui-slots. These are peer dependencies of a dependency, not build packages. The headless profile cannot resolve these because they are UI packages that require a web/TUI plane. No build packages are required by the latest retry, and the unresolved edges are not build-approval-required. Therefore, a headless retry cannot fix this; it requires a different execution plane. |
| `dsh-skills-manager-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin loaded but the DSH runtime did not resolve the peer dependency @deepseek-ai/dsh-client-ui-slots, which is required by the plugin's manifest. The missing peer is a runtime contract that cannot be satisfied by approving build packages, and no build packages are required. A headless retry cannot resolve this missing peer dependency. |
| `dsh-thirteen-bg-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin's peer dependency @deepseek-ai/dsh-client-ui-slots is missing in the runtime graph, and the required version range is unsatisfiable in the current DSH runtime. No build packages are required, so a retry cannot resolve the missing peer. The issue is a headless contract mismatch: the plugin expects a UI slots package that the headless runtime does not provide. |
| `dsh-tui-node22` | `build-approval-required` | `retry-headless` | `build-approval` | approve `@google/genai`, `protobufjs` |
|  |  |  |  | The observed result is build-approval-required for @google/genai and protobufjs. The repository README explicitly documents that these packages require build approval and recommends setting allowBuilds to false, indicating they are safe to approve. No previous approvals exist, so retrying with these two packages added is justified. |
| `dsh-univer-office-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin's peer dependencies require exact versions (0.1.0-rc.8) of DSH packages, but the headless environment resolves 0.1.1-rc.2, causing mismatches. No build packages are required, and the issue is a version contract mismatch that cannot be resolved by approving builds. |
| `dsh-vision-toolkit-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin requires @deepseek-ai/dsh-client-ui-primitives and @deepseek-ai/dsh-client-ui-slots as peer dependencies, but the headless DSH runtime does not provide them. The plugin declares a web client platform and injects client UI modules, indicating it is designed for a Web/TUI plane. The headless profile cannot satisfy these runtime peer contracts, and no build packages are required or approved. Retrying headless would not resolve the missing runtime peers. |
| `dsh-wallpaper-engine-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin loaded but the DSH runtime did not resolve the peer dependency @deepseek-ai/dsh-client-ui-slots, which is required by the plugin's manifest. The headless profile cannot provide this UI slot package, and no build packages are required or approved. Retrying headless would not change the missing peer resolution. |
| `dsh-web-ui-all-node22` | `build-approval-required` | `retry-headless` | `build-approval` | approve `cloudflared`, `cpu-features`, `node-pty`, `ssh2` |
|  |  |  |  | The plugin requires approval for four dependency builds. The repository README explicitly instructs adding cloudflared, cpu-features, and ssh2 to pnpm-workspace.yaml allowBuilds, and node-pty is a common transitive dependency for terminal functionality. All four are exact observed build packages, so approving them is justified. |
| `dshscan-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin's peer dependency @deepseek-ai/dsh-tools requires exactly 0.1.0-rc.6, but the headless DSH runtime provides 0.1.1-rc.2, causing a runtime-import-observed mismatch. No build packages are required, and the mismatch is a version contract issue that cannot be resolved by approving builds. The headless profile cannot change the runtime version, so retrying would not help. |
| `openpencil-node24` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin's peer dependency @deepseek-ai/dsh-client-ui-slots is missing in the runtime graph, and the plugin declares a web client platform with inject list that does not include this package. The headless profile cannot resolve this peer contract because it requires a web/TUI plane to provide the UI slots package. No build packages are required, so a retry cannot fix the missing peer. |
| `sanqi-market-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin's peer dependency @deepseek-ai/dsh-client-ui-slots is missing in the headless runtime, but the plugin declares a web client platform and requires a web UI slots package that the headless DSH profile cannot provide. The missing peer is a UI slots contract, not a build approval issue. No build packages are required, so retrying headless cannot resolve the missing peer dependency. |
| `whale-on-desk-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin's peer dependency @deepseek-ai/dsh-client-ui-slots is not resolved by the DSH runtime, and the plugin declares a web client platform with injection of @deepseek-ai/dsh-client-runtime. The headless profile cannot satisfy the missing UI slots peer, and no build packages are required or approved. Retrying headless would not resolve the missing peer contract. |

A stopped plan is not a compatibility failure. It means this headless-only milestone has no Agent-supported retry to execute.
