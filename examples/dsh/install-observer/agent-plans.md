# DSH headless Agent review

Updated: 2026-09-18T11:38:20.178Z

The Agent reads bounded repository evidence and the latest isolated headless result. There is no static environment-planning fallback. Only an exact observed build-package name can reach the no-secret retry runner.

- Current review set: 16
- Agent-reviewed: 13
- Agent failures awaiting retry: 3

| Case | Previous evidence | Agent action | Classification | Retained build policy |
| --- | --- | --- | --- | --- |
| `anweat-dsh-browser-node22` | `build-approval-required` | `agent-failed` | `unknown` | none |
|  |  |  |  | a stopped headless plan cannot approve dependency builds |
| `better-sidebar-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The artifact installed and loaded, but the headless runtime did not resolve the plugin's client UI peer contracts: @deepseek-ai/dsh-client-ui-primitives and @deepseek-ai/dsh-client-ui-slots are missing, and react/react-dom are missing, with runtime-import-observed usage. This is a peer/runtime resolution contract failure, not a pnpm build-approval gate; no build packages are required or previously approved, so a headless retry that only edits the dependency-build approval list cannot change the outcome. |
| `dsh-auxiliary-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The reproduced failure is a peer-contract incompatibility, not a build-approval gate. The exact artifact installed and loaded, but the runtime graph shows @dsh-plugin/dsh-loader@^1.3.0 as a missing peer with runtime-import-observed usage, so the plugin's runtime import cannot resolve in the headless DSH profile. No build packages are required or previously approved, and the runner may only change the pnpm dependency-build approval list, which cannot supply a missing runtime peer. A retry would not change the outcome. |
| `dsh-deepread-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The reproduced failure is a peer-contract incompatibility, not a build-approval gate. The artifact installed and loaded, but the DSH headless runtime did not resolve the declared peer react@^18.2.0 (runtime-import-observed), leaving one missing peer contract. No build packages are required or previously approved, and the runner may only edit the pnpm dependency-build approval list, which cannot supply a missing runtime peer. A retry would not change this outcome. |
| `dsh-free-vision-node22` | `build-approval-required` | `agent-failed` | `unknown` | none |
|  |  |  |  | a stopped headless plan cannot approve dependency builds |
| `dsh-full-remote-node22` | `peer-contract-incompatible` | `stop-headless` | `different-plane` | none |
|  |  |  |  | The reproduced result is peer-contract-incompatible, not build-approval-required: the artifact installed and loaded, and the runtime graph shows four unresolved peer contracts (@deepseek-ai/dsh-client-runtime, @deepseek-ai/dsh-client-ui-slots, react, react-dom) that the DSH runtime does not provide. No build packages are required or previously approved, so there is nothing for the dependency-build approval list to add. The plugin's own manifest and README declare it targets the Harness web profile (dsh.client.platform=web, inject of client-runtime/ui-slots, depends on webServer and Host connection services) and explicitly states it is not intended for headless profiles, so the missing peers are a plane mismatch rather than an approval gap. |
| `dsh-git-worktree-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The reproduced failure is a peer-contract incompatibility, not a build-approval gate. The exact artifact installed and loaded, but the runtime graph shows @deepseek-ai/dsh-client-locale resolved to 0.1.5-rc.2 while the manifest pins the peer to exactly 0.1.2-rc.1, and react is missing as a peer. No build packages are required or previously approved, so there is nothing for the runner's only allowed lever (the pnpm dependency-build approval list) to change. A headless retry cannot alter peer resolution or supply the missing react peer, so it would reproduce the same contract mismatch. |
| `dsh-history-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The reproduced result is peer-contract-incompatible, not build-approval-required. The exact artifact installed and loaded, but the DSH runtime did not resolve the declared peer @deepseek-ai/dsh-client-runtime ( =0.0.1-rc.1 0.1.0 =0.1.0-rc.1 0.2.0-0); the runtime graph shows it missing with no-literal-reference-observed, and react is also missing with runtime-import-observed. No build packages are required or were previously approved, so there is nothing to add to the dependency-build approval list. The plugin's dsh.client.platform is web and its inject list targets client-runtime/UI packages, which is a different execution plane than the existing headless DSH profile. A headless retry cannot change the approval list to fix an unresolved peer contract, so stop. |
| `dsh-notifier-node22` | `build-approval-required` | `agent-failed` | `unknown` | none |
|  |  |  |  | a stopped headless plan cannot approve dependency builds |
| `dsh-plugin-writing-guard-node22` | `build-approval-required` | `stop-headless` | `insufficient-evidence` | none |
|  |  |  |  | The observed result is build-approval-required for the package dsh-plugin-writing-guard, but the repository evidence does not support approving that package as a dependency build. The manifest declares no install/build lifecycle that would justify a build approval for the plugin itself; the only lifecycle script is a postinstall (node scripts/install-extras.mjs), and the required build packages list names the plugin package itself rather than a distinct native/build dependency. The dynamic evidence shows the plugin's peer dependencies (@deepseek-ai/cordis, @deepseek-ai/dsh-llm, @deepseek-ai/dsh-tools) are all satisfied in the runtime graph, with zero mismatched/missing/indeterminate peer contracts, so there is no newly supported build package to add. A retry would only re-approve the same single package with no repository basis, so no additional headless retry is justified. |
| `dsh-thirteen-bg-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The reproduced failure is a peer-contract incompatibility, not a build-approval gate. The artifact installed and loaded, but three declared peer dependencies (@deepseek-ai/dsh-client-runtime, @deepseek-ai/dsh-client-ui-slots, react) are unresolved/missing in the headless runtime graph, and the plugin is a web-platform client plugin whose declared inject list targets the Web GUI plane. No build packages are required or previously approved, so there is nothing to approve and no headless retry can change the peer resolution outcome. |
| `dsh-vision-proxy-node22` | `build-approval-required` | `retry-headless` | `build-approval` | approve `dsh-vision-proxy`, `sharp` |
|  |  |  |  | The reproduced headless result is build-approval-required for exactly two packages, dsh-vision-proxy and sharp, both of which were already approved in earlier retries, so no earlier approval is dropped and no new package is introduced. Repository evidence supports approving both: package.json declares a postinstall script (node scripts/postinstall.js) for the plugin itself and lists sharp under optionalDependencies with a pnpm.onlyBuiltDependencies entry of ["sharp"], and the README documents the pnpm = 10 ignored-build-scripts prompt for exactly 'dsh-vision-proxy, sharp' with an allowBuilds example enabling both. The bounded dynamic evidence shows a resolved profile lockfile (35 nodes, 0 unresolved) and runtime graph (509 nodes, 0 unresolved) on DSH 0.1.5-rc.2 / Node 22, with no plugin peer contract mismatches, so the only remaining gate is the explicit dependency-build approval list, which the runner is permitted to change. |
| `dshscan-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The artifact installed and loaded, but the failure is a peer-contract mismatch: the plugin declares peerDependency @deepseek-ai/dsh-tools@0.1.0-rc.6 while the host DSH 0.1.5-rc.2 resolves @deepseek-ai/dsh-tools@0.1.5-rc.2, and the runtime import is observed. No build packages are required or were previously approved, so there is nothing to add to the pnpm dependency-build approval list; the runner's only permitted change cannot alter peer version resolution. A retry would reproduce the same mismatch, so stop. |
| `openpencil-node24` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The reproduced result is peer-contract-incompatible, not build-approval-required: the artifact installed and loaded, and the runtime graph shows 10 of 14 declared peer contracts satisfied with 4 missing (@deepseek-ai/dsh-client-runtime, @deepseek-ai/dsh-client-ui-slots, react, react-dom). No build packages are required or previously approved, so the only permitted runner change (the pnpm dependency-build approval list) cannot affect this outcome. The missing peers are runtime/type resolution gaps in the headless profile, not native build scripts, so a retry would reproduce the same contract failure. |
| `sanqi-market-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The reproduced failure is a peer-contract incompatibility, not a build-approval gate. The artifact installed and loaded, but three declared peer dependencies (@deepseek-ai/dsh-client-runtime, @deepseek-ai/dsh-client-ui-slots, react) remain unresolved/missing in the runtime graph, and the runner may only modify the pnpm dependency-build approval list. No build packages are required or previously approved, so no approval-list change can resolve missing host-provided peers. Retry is not justified. |
| `whale-on-desk-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The artifact installed and loaded, but the failure is a peer-contract mismatch in the headless runtime, not a build-approval gate. The plugin declares five peer dependencies; the runtime graph shows @deepseek-ai/dsh-client-runtime, @deepseek-ai/dsh-client-ui-slots, and react as missing, with react additionally showing runtime-import-observed. No build packages are required or previously approved, so there is nothing to approve and no subset of observed build packages to add. The plugin's dsh.client.platform is web and its client injects @deepseek-ai/dsh-client-runtime, which is a browser/web plane dependency unavailable in the existing headless DSH profile; approving builds cannot supply it. Retry-headless is therefore not justified. |

A stopped plan is not a compatibility failure. It means this headless-only milestone has no Agent-supported retry to execute.
