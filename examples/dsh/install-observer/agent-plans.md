# DSH headless Agent plans

Updated: 2026-08-24T02:53:10.884Z

The Agent reads bounded repository evidence and the latest isolated headless result. There is no static environment-planning fallback. Only an exact observed build-package name can reach the no-secret retry runner.

| Case | Previous evidence | Agent action | Classification | Headless delta |
| --- | --- | --- | --- | --- |
| `better-sidebar-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web platform client with peer dependencies on DSH client UI packages, but the headless DSH profile cannot provide the required web client runtime. The observed failure is a runtime import of @deepseek-ai/dsh-client-ui-primitives, which is a web-only package not available in the headless environment. Retrying with build approvals would not resolve this contract mismatch. |
| `dsh-agency-agents-node22` | `peer-contract-incompatible` | `stop-headless` | `different-plane` | none |
|  |  |  |  | The plugin declares a web client platform and injects client UI packages, but the headless DSH profile cannot provide the required web client runtime. The observed failure is a peer-contract incompatibility because @deepseek-ai/dsh-client-ui-slots@^0.1.0-rc.5 is not resolved by the headless runtime, which is expected since the plugin targets the web plane. No build packages were observed, so there is nothing to approve for a retry. |
| `dsh-archive-manager-node22` | `peer-contract-incompatible` | `stop-headless` | `different-plane` | none |
|  |  |  |  | The plugin declares a web client platform and injects client UI packages, but the headless DSH profile cannot provide the required web client runtime. The observed failure is a missing peer dependency (@deepseek-ai/dsh-client-ui-primitives) that is only available in a web environment, not a headless one. No build packages were observed, so a build-approval retry is not applicable. |
| `dsh-awiki-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and requires @deepseek-ai/dsh-client-ui-primitives at runtime, which the headless DSH profile does not provide. The observed failure is a runtime import of a UI primitive package, not a build approval issue. Retrying headless cannot resolve the missing UI dependency. |
| `dsh-codex-connect-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin's peer dependency on @deepseek-ai/dsh-agent is pinned to 0.1.1-rc.2, but the observed DSH version is 0.1.1-rc.2, which does not satisfy the required 0.1.0-rc.7. This is a peer contract incompatibility, not a build approval issue. No build packages were observed, so retry-headless is not applicable. |
| `dsh-codex-subscription-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and injects UI components, which cannot be satisfied by the headless DSH profile. The observed failure is a runtime import of a UI primitives package, indicating the plugin requires the web/TUI plane. No build packages were observed, so no dependency approval can be granted. |
| `dsh-coding-subscription-oauth-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and requires a Web/TUI plane, which is outside the headless DSH profile's execution plane. The observed peer-contract-incompatible result is due to a runtime import mismatch with @deepseek-ai/dsh-atomic-write, but the plugin's manifest and README indicate it is designed for the web profile, not headless. No build packages were observed, so there is nothing to approve. |
| `dsh-commandcode-provider-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and injects client modules, but the observed failure is a runtime peer dependency resolution issue for @deepseek-ai/dsh-client-ui-primitives, which is not a build-time package. The headless profile cannot resolve runtime peer dependencies or provide the required web client environment. No build packages were observed, so no build approval is applicable. |
| `dsh-config-manager-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and injects client runtime packages, but the observed failure is a runtime peer resolution issue for @deepseek-ai/dsh-client-ui-slots, which is not a build-time dependency. The headless profile cannot resolve runtime peer contracts, and no build packages were observed to approve. |
| `dsh-free-vision-node22` | `build-approval-required` | `retry-headless` | `build-approval` | approve `dsh-free-vision` |
|  |  |  |  | The observed build-approval-required result is due to the plugin package dsh-free-vision requiring explicit approval for its dependency build. The repository evidence (package.json) confirms the package name and version, and the manifest shows it is a valid DSH plugin with a dsh bundle patch. The only build package observed is dsh-free-vision itself, which matches the plugin name. Approving this package is consistent with the headless profile and does not introduce additional dependencies or services. |
| `dsh-full-remote-node22` | `peer-contract-incompatible` | `stop-headless` | `different-plane` | none |
|  |  |  |  | The plugin requires a web profile and depends on @deepseek-ai/dsh-client-ui-slots, which is not available in the headless DSH profile. The observed peer-contract-incompatible result is due to the missing UI slots package, which cannot be resolved in the headless environment. Retrying headless would not resolve the missing dependency. |
| `dsh-mnemon-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web platform and injects client UI packages, but the headless profile cannot resolve the required peer dependency @deepseek-ai/dsh-client-ui-primitives. The observed failure is a peer-contract incompatibility, not a build approval issue. No build packages were observed, so retry-headless is not applicable. |
| `dsh-pet-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and requires @deepseek-ai/dsh-client-ui-slots as a peer dependency, but the headless DSH profile cannot resolve or provide UI slot modules. The observed failure is a peer-contract incompatibility due to missing UI slots, which is outside the headless execution plane. |
| `dsh-skills-manager-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and requires @deepseek-ai/dsh-client-ui-slots, which the headless DSH profile does not provide. The observed failure is a peer-contract incompatibility due to the missing UI slots dependency, not a build approval issue. |
| `dsh-thirteen-bg-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and injects client-side UI modules, but the observed failure is a peer dependency resolution issue for @deepseek-ai/dsh-client-ui-slots, which is not a build approval matter. The headless profile cannot resolve or approve runtime peer dependencies, and no build packages were observed. |
| `dsh-univer-office-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and requires a browser-based Gateway and Viewer, which cannot be satisfied by the headless DSH profile. The observed peer dependency mismatch is a symptom of the headless environment not providing the required web client runtime. |
| `dsh-vision-proxy-node22` | `build-approval-required` | `retry-headless` | `build-approval` | approve `dsh-vision-proxy` |
|  |  |  |  | The observed result is build-approval-required for the package dsh-vision-proxy. The repository evidence (package.json) explicitly lists dsh-vision-proxy in the pnpm.onlyBuiltDependencies array, indicating the package itself has a postinstall script that requires build approval. The README also instructs users to approve dsh-vision-proxy and sharp in the profile's pnpm-workspace.yaml. Since the observed build packages only include dsh-vision-proxy (not sharp, which is optional), approving dsh-vision-proxy is sufficient to proceed with the headless profile. The plugin is designed for headless use and does not require a Web/TUI plane, secrets, services, or system packages. |
| `dsh-vision-toolkit-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and injects UI packages, but the observed failure is a runtime import of @deepseek-ai/dsh-client-ui-primitives not being resolved by the headless DSH runtime. No build packages were observed, so there is nothing to approve for a build retry. The issue is a headless contract mismatch: the plugin requires web UI primitives that the headless profile does not provide. |
| `dsh-wallpaper-engine-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin loaded but requires @deepseek-ai/dsh-client-ui-slots@ =0.1.0-rc.6 which is not resolved by the headless DSH runtime. The plugin's client injects @deepseek-ai/dsh-client-runtime and targets the web platform, indicating it is designed for the web GUI plane, not headless. No build packages were observed, so no dependency-build approval is applicable. |
| `dshscan-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin's peer dependency requires @deepseek-ai/dsh-tools@0.1.0-rc.6, but the installed DSH runtime provides 0.1.1-rc.2, causing a runtime-import-observed incompatibility. No build packages were observed, so no dependency-build approval can be granted. The issue is a version mismatch in the peer contract, not a build approval requirement. |
| `graphflow-node22` | `build-approval-required` | `retry-headless` | `build-approval` | approve `@roarpeng/graphflow` |
|  |  |  |  | The observed build-approval-required result is for the exact package @roarpeng/graphflow, which is the plugin itself. The repository evidence (package.json, README, cordis.patch.yml) confirms this package is a legitimate dsh plugin with a proper dsh bundle and no suspicious build steps. The build approval is limited to the observed package, and the headless profile can proceed with the retry. |
| `openpencil-node24` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and injects UI packages, but the observed failure is a peer-contract incompatibility: @deepseek-ai/dsh-client-ui-slots@^0.1.0-rc.6 was not resolved by the DSH runtime. No build packages were observed, so no dependency-build approval can address the missing peer resolution. The headless profile cannot provide the required web client runtime. |
| `sanqi-market-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and requires @deepseek-ai/dsh-client-ui-slots peer dependency, which is not resolvable in the headless DSH runtime. The observed result is peer-contract-incompatible with no build packages, so a retry with build approvals is not applicable. |
| `whale-on-desk-node22` | `peer-contract-incompatible` | `stop-headless` | `headless-contract` | none |
|  |  |  |  | The plugin declares a web client platform and injects @deepseek-ai/dsh-client-runtime, but the headless DSH profile cannot resolve @deepseek-ai/dsh-client-ui-slots, which is a required peer dependency. The observed failure is a peer-contract incompatibility, not a build approval issue. No build packages were observed, so retry-headless is not applicable. |

A stopped plan is not a compatibility failure. It means this headless-only milestone has no Agent-supported retry to execute.
